We rewriting Jotunn to use C modules bby

Dependencies:

- GLFW
- GLEW
- stb_image
- freetype2

One of these days I will write some real documentation, but for now just type `make debug` in the root of the repo. TestApp outputs to `./TestApp/.build/apps/TestApp` :)